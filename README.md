# TRAKKR-Updates

Public firmware update releases for TRAKKR devices.

This repository is used only for compiled OTA firmware binaries. Source code is maintained separately and is not published here.

Each release contains a `firmware.bin` asset used by TRAKKR devices for automatic updates.
